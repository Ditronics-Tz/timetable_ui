# UI only. Backend stack: see Ditronics-Tz/Sacas-backend docker-compose.yml
services:
  web:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8080/api}
    ports:
      - "3000:80"
